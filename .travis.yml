## Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
dist: xenial
os:
  - linux
  - osx
julia:
  - 1.0
  - 1.1
  - 1.2
  - nightly
notifications:
  email: false
git:
  depth: 99999999

## uncomment the following lines to allow failures on nightly julia
## (tests will run but not make your overall status red)
#matrix:
#  allow_failures:
#  - julia: nightly

## uncomment following lines to deploy documentation
jobs:
  include:
    - stage: "Documentation"
      julia: 1.7
      os: linux
      script:
        - julia --project=docs/ -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd()));
            Pkg.instantiate()'
        - julia --project=docs/ docs/make.jl
      after_success: skip
after_success:
  - julia --project=test/coverage -e 'using Pkg; Pkg.instantiate()'
  - julia --project=test/coverage test/coverage/coverage.jl
  - julia -e 'Pkg.add("Documenter")'

## Same as BanditOpt.jl
## uncomment the following lines to override the default test script
#script:
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("BanditOpt"); Pkg.test("BanditOpt"; coverage=true)'
#after_success:
  # push coverage results to Coveralls
#  - julia -e 'cd(Pkg.dir("Jchemo")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
  # push coverage results to Codecov
#  - julia -e 'cd(Pkg.dir("Jchemo")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
  # Deploy Docs
#  - julia -e 'Pkg.add("Documenter")'
#  - julia -e 'cd(Pkg.dir("Jchemo")); include(joinpath("docs", "make.jl"))'

